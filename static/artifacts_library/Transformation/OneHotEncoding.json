{
  "Train": [
    "# Data treatment\n",
    "# Transformation/OneHotEncoding\n",
    "\n",
    "def encoder_train(obj, x_data):\n",
    "    obj.fit(x_data)\n",
    "    return obj.transform(x_data)\n",
    "\n",
    "encoder = OneHotEncoder(handle_unknown='ignore')\n",
    "x_train_transformed = encoder_train(encoder, x_train_augmented)"
  ],
  "Valid": [
    "# Data treatment\n",
    "# Transformation/OneHotEncoding\n",
    "\n",
    "def encoder_valid(obj, x_data):\n",
    "    return obj.transform(x_data)\n",
    "\n",
    "x_valid_transformed = encoder_valid(encoder, x_valid_augmented)"
  ],
  "Test": [
    "# Data treatment\n",
    "# Transformation/OneHotEncoding\n",
    "\n",
    "def encoder_test(obj, x_data):\n",
    "    return obj.transform(x_data)\n",
    "\n",
    "x_test_transformed = encoder_test(encoder, x_test_augmented)"
  ],
  "Imports": ["from sklearn.preprocessing import OneHotEncoder\n"]
}
