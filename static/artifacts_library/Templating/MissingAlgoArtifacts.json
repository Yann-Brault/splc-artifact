{
  "Train": [
    "# Model training\n",
    "# <To complete> Algo/Name [Algo/MLP, Algo/SVM, Algo/KNN; ...]\n",
    "\n",
    "# <To modify>\n",
    "def object_train(obj, x_data, y_data):\n",
    "    return obj.fit(x_data, y_data)\n",
    "\n",
    "# <To modify>\n",
    "object = class_constructor(arg0, arg1)\n",
    "# <To modify>\n",
    "model = object_train(object, x_train, y_train)"
  ],
  "Valid": [
    "# Prediction\n",
    "# <To complete> Algo/Name [Algo/MLP, Algo/SVM, Algo/KNN; ...]\n",
    "\n",
    "# <To modify>\n",
    "def object_predict(obj, x_data):\n",
    "    return obj.predict(x_data)\n",
    "\n",
    "# <To modify>\n",
    "y_pred_valid = object_predict(object, x_valid)"
  ],
  "Test": [
    "# Prediction\n",
    "# <To complete> Algo/Name [Algo/MLP, Algo/SVM, Algo/KNN; ...]\n",
    "\n",
    "# <To modify>\n",
    "def object_predict(obj, x_data):\n",
    "    return obj.predict(x_data)\n",
    "\n",
    "# <To modify>\n",
    "y_pred = object_predict(object, x_test)"
  ],
  "Imports": ["# <To modify> add the correct import for your treatment\n"]
}
