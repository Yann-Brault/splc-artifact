{
  "Train": [],
  "Valid": [
    "# Evaluation\n",
    "# Metrics/Recall\n",
    "\n",
    "def generated_recall_score_valid(y_data, pred):\n",
    "    return f1_score(y_data, pred ,average='macro')\n",
    "\n",
    "valid_score = generated_recall_score_valid(y_val, y_pred_valid)\n",
    "print(f'score: {valid_score}')\n",
    "\n",
    "# suggestion: visualise your data here"
  ],
  "Test": [
    "# Evaluation\n",
    "# Metrics/Recall\n",
    "\n",
    "def generated_recall_score(y_data, pred):\n",
    "    return f1_score(y_data, pred, average='macro')\n",
    "\n",
    "score = generated_recall_score(y_test, y_pred)\n",
    "print(f'score: {score}')\n",
    "\n",
    "# suggestion: visualise your data here"
  ],
  "Imports": ["from sklearn.metrics import recall_score\n"]
}
