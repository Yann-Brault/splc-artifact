{
  "Train": [],
  "Valid": [
    "# Evaluation\n",
    "# Metrics/ROC_curve\n",
    "\n",
    "def generated_roc_curve_valid(y_data, pred):\n",
    "    return roc_curve(y_data, pred, pos_label=2)\n",
    "\n",
    "fpr_valid, tpr_valid, thresholds_valid = generated_roc_curve_valid(y_val, y_pred_valid)\n",
    "auc_valid = auc(fpr_valid, tpr_valid)\n",
    "print(f'thresholds: {thresholds_valid}, auc: {auc_valid}')\n",
    "\n",
    "# suggestion: visualise your data here"
  ],
  "Test": [
    "# Evaluation\n",
    "# Metrics/ROC_curve\n",
    "\n",
    "def generated_roc_curve(y_data, pred):\n",
    "    return roc_curve(y_data, pred, pos_label=2)\n",
    "\n",
    "fpr, tpr, thresholds = generated_roc_curve(y_test, y_pred)\n",
    "auc_score = auc(fpr, tpr)\n",
    "print(f'thresholds: {thresholds}, auc: {auc_score}')\n",
    "\n",
    "# suggestion: visualise your data here"
  ],
  "Imports": [
    "from sklearn.metrics import roc_curve\n",
    "from sklearn.metrics import auc\n"
  ]
}
