{
  "Train": [
    "# Model training\n",
    "# Algo/KNN\n",
    "\n",
    "def neigh_train(obj, x_data, y_data):\n",
    "    return obj.fit(x_data, y_data)\n",
    "\n",
    "neigh = KNeighborsClassifier(n_neighbors=3)\n",
    "model = neigh_train(neigh, x_train_transformed, y_train_augmented)"
  ],
  "Valid": [
    "# Prediction\n",
    "# Algo/KNN\n",
    "\n",
    "def neigh_predict(obj, x_data):\n",
    "    return obj.predict(x_data)\n",
    "\n",
    "y_pred_valid = neigh_predict(neigh, x_valid_transformed)"
  ],
  "Test": [
    "# Prediction\n",
    "# Algo/KNN\n",
    "\n",
    "def neigh_predict(obj, x_data):\n",
    "    return obj.predict(x_data)\n",
    "\n",
    "y_pred = neigh_predict(neigh, x_test_transformed)"
  ],
  "Imports": ["from sklearn.neighbors import KNeighborsClassifier\n"]
}
